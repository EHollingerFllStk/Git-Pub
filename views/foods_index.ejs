<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
    <title>Git Pub Food</title>
</head>

<body>
    <h1>Git Pub Food</h1>
    <a href="/drinks/">Drinks</a>
    <ul>
        <% for(let i=0; i < allFoods.length; i++) { %>
            <li>
                <a href="/foods/<%=i%>">
                    <%= allFoods[i].name.charAt().toUpperCase()+allFoods[i].name.slice(1) %>
                </a>
            </li>
            <% } %>
    </ul>

</body>

</html>